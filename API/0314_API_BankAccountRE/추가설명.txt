모달
proc
 - static으로 param으로 넘어올 값을 저장할 변수 생성
 - init 부분에서 static 변수에 param값을 넘기기
main
 - 공유할 데이터
 - 윈도우 띄우는 함수 + param으로 공유 데이터 넘기기
 - if (ret==IDOK){}


팝업은 정보를 가져올뿐 처리를 하면 안된다.

//==================================

