도형
-SHAPE
	POINT
	int

=================
1. struct 만들 때 방법
이름은 접두에 tag를 붙인다
별명으로 이름과, 포인터 이름을 만들어 놓는다.

=================
2. keyboard 입력받는 방법
포커싱의 기준 - 클릭 후 활성화 된 윈도우
kedown은 sacncode, vkey값만을 다룰 수 있다.
====================
API 디버깅 팁
GUI 코드를 이용ㅎ ㅐ쓰레기를 사용할 경우 디버깅이 굉장히 어렵다.
따라서
1.기본 제공 하는 코드중 consol 부분을 코드를 추가함
2.AllocConsole

3.freopne 함수

=================
4, 마우스 캡쳐
윈도우 영역을 벗어나도 메세지를 받을 수 있도록 하는 함수
SetCapture
ReleaseCapture

getCapture 현재 누가 마우스를 캡쳐하고 있는지 찾는 함수 반환 값이 HWND 핸들값이다
===================
5. 논리 좌표 물리좌표
lParam을 통해 들어오는 값은 물리 좌표다
하지만 GDI 객체는 논리 좌표를 이용한다.

논리 좌표가 뭐야 도대체.

근데 변환 없이 잘써지는 이유
논리 좌표 변환 모드 디폴트 값이 논리좌표와 같기 때문=똑같이 픽셀을 단위로 작성되어 있기 때문

만약 맵핑 모드가 달라질 경우 서로 변환 해주어야 원하는 형태로 출력이 가능하다.

해상도와 관계없이 정확한 그림을 출력 하고 싶을 때 사용한다.


